# VPS Audit 2026-04-19

## Scope

First real VPS audit for the control plane. Goal was to replace assumptions in topology with verified runtime facts.

## Verified Facts

- The live bot runtime is a Docker Compose stack at `/home/samhcharles/srv/bots/chopsticks-lean/docker-compose.yml`
- Running containers during audit:
  - `chopsticks-lean-bot`
  - `chopsticks-lean-postgres`
  - `chopsticks-lean-redis`
- The bot exposes host port `9100` for health or metrics
- The live bot repo is a Git working tree at `/home/samhcharles/srv/bots/chopsticks-lean`
- The live bot repo points at `https://github.com/samhcharles/chopsticks-lean`
- The live bot repo was on `main` and ahead of `origin/main` by 1 commit during the audit
- Backup scripts exist in the bot repo and default to `./data/backups` unless overridden
- Shared backup root exists at `/home/samhcharles/srv/backups`
- Shared manual backup path exists at `/home/samhcharles/srv/backups/manual`
- Shared service root exists at `/home/samhcharles/srv`
- Shared logs root exists at `/home/samhcharles/srv/logs`
- Proxy directory exists at `/home/samhcharles/srv/proxy/caddy`, but no config files were present there during audit
- No user crontab entries were present during the audit
- Initial timer pass only showed standard system timers
- Observed runtime-adjacent directories:
  - `/home/samhcharles/srv/agents/nqita`
  - `/home/samhcharles/srv/volumes/chopsticks`
  - `/home/samhcharles/srv/volumes/nqita`

## Gaps

- Reverse proxy ownership and domain routing are still unknown
- The exact role of `nqita` under `/home/samhcharles/srv/agents` is not yet documented
- The exact ownership and usage of `/home/samhcharles/srv/volumes/*` is not yet documented
- No custom service manager was verified for the Compose stack beyond the containers themselves
- Backup automation scheduling is not yet verified

## Recommended Follow-Up

1. Audit `/home/samhcharles/srv/logs`, `/home/samhcharles/srv/agents/nqita`, and `/home/samhcharles/srv/volumes/*` in more depth
2. Determine whether Caddy is intentionally unused, externally managed, or simply undocumented
3. Decide whether the live bot should keep being deployed from an in-place Git working tree on the VPS
4. Add a scripted inventory snapshot so topology drift is easier to detect
